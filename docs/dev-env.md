# Development environment

_How to start the dev environment, and whether reproduction is expected
of the agent. This package ships a Pi extension, so the dev loop will
revolve around loading a built extension into Pi; fill in concrete
commands when implementation starts._

## Starting

- _To be filled in (e.g. `devenv shell`, `nix develop`, or just "plain
  Node 22 on PATH")._

## Reproduction

- **Do not attempt AI reproduction yet.** There is no runnable code.
  When the extension exists, this section records the one command that
  loads it into Pi and reproduces a reported bug (or an explicit
  statement that reproduction requires a human, e.g. interactive UI
  gates, which pi-reins has by design).

# Development environment

_How to start the dev environment, and whether reproduction is expected
of the agent. Fill this in as the project takes shape._

## Starting

- _To be filled in (e.g. `devenv shell`, `nix develop`, `docker compose
  up`, or just "plain Node 22 on PATH")._

## Reproduction

- **Do not attempt AI reproduction yet.** The project has no runnable
  application so far. When it gains one, this section records the one
  command that reproduces a reported bug (or an explicit statement that
  reproduction requires a human, e.g. an external service or hardware).
